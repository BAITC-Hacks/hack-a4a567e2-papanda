# Проверка на 2026-09-21

Статус всего эксперимента: **NOT READY** — диалектические проверки ожидают
совместной спецификации. Базовый сценарий отдельно запущен и проверен.

## Локальные тесты

- Python 3.12; openai-agents 0.22.3; openai 3.16.2; pydantic 2.13.5.
- `python -m pytest -q`: 18 passed, 4 subtests passed (1.87 s).
- Финальный повтор перед отправкой, `python -m pytest -q -p no:cacheprovider`:
  18 passed, 4 subtests passed (1.70 s), без предупреждений.
- Первоначальный запуск: 15 passed, 3 ошибки доступа pytest к временной папке.
  Повтор с разрешением доступа: все тесты прошли; предупреждение о pytest cache
  не относится к поведению приложения.
- Проверены исходные строки, ошибки схемы/таймауты/повтор, непустой ввод,
  сохранение частичного результата, редактирование секретов в логах,
  независимые копии baseline для двух проверок и отсутствие ложного успеха.
- Это офлайн-симуляции ответов; они не доказывают качество живой модели.
- CLI `--help` работает; отсутствующий файл даёт явную ошибку без результата.

## Реальные прогоны через Groq

Модель: `openai/gpt-oss-120b`. Во всех случаях используется OpenAI Agents SDK.
Режим структурирования: json_prompt, затем локальная проверка Pydantic.

1. `runs/20260921T142052Z-295745d9`: пять APIConnectionError, сеть песочницы
   недоступна. Отказ записан явно; подставных ответов нет.
2. `runs/20260921T142129Z-20e2c3d7`: пять ответов, но ручная проверка выявила
   придуманные требования к справке, медицинскую специализацию консультации,
   расположение парковки у входа. **Качество этого прогона неприемлемо.**
3. После усиления ограничения на факты:
   `runs/20260921T142346Z-fc9b8e43`: 5 успешных ответов, 0 технических ошибок,
   5.35 секунды обработки, 3960 токенов по usage провайдера.
   Категории: справка, жалоба, другое, жалоба, справка.
   Ручная проверка: выдуманные процедуры, адреса и область консультации исчезли;
   ответы запрашивают недостающие сведения. Это один прогон, не гарантия.

Все исходные прогоны сохранены локально без перезаписи, включая неудачные.
SDK remote tracing отключён. Трассы не включены в Git.

## Ограничения

Проверки DialecticAI и с нуля ещё не реализованы; результаты их сравнения отсутствуют.
Предварительная структура данных не доказывает диалектической корректности.
У существующего фреймворка обнаружено обязательное противоречие/скачок перед
исполнением. Этот вопрос ещё не решён. Реальное отсутствие ключа/лимиты провайдера
могут препятствовать запуску. Исходный промпт был недостаточен; расширенная
проверка качества на новых обращениях ещё не проводилась.
